import _ from'lodash';export default function(stops,opacity){if(isNaN(opacity)){opacity=1;}return _.reduce(stops,function(ret,color,key){ret[key]=color.alpha(color.alpha()*opacity).rgbaString();return ret;},{});}
//# sourceMappingURL=stopsOpacity.js.map